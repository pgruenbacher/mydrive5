.modal-content
  .modal-header
    button.btn.btn-default.pull-right(type='button', data-dismiss='modal')
      span(aria-hidden='true') ×
      span.sr-only Close
    a.btn.btn-default.pull-right(ng-click="disable(true)",ng-show="!button.disabled") Disable (won't appear on website)
    a.btn.btn-default.pull-right(ng-click="disable(false)",ng-show="button.disabled") Enable (will appear on website)
    h4.modal-title {{button.title}}
  .modal-body
    input(type="text",ng-model="button.title")
    div.button-navbar
      ul
        li
          span Choose action of button:
        li
          a(ng-click="select('Navigation')",ng-class="{active:selected=='Navigation'}") Navigate to page
        li
          a(ng-click="select('Action')",ng-class="{active:selected=='Action'}") Open Form

    .divider      
    div(ng-switch="selected")
      div(ng-switch-when="Navigation")
        h4 Choose button navigation destination
        ul.item-list
          li(ng-repeat="item in available",ng-class="{highlight:button.perform===item}")
            a(ng-click="setDestination(item)") {{item}}
      div(ng-switch-when="Action")
        h4 Choose button action
        ul.item-list
          li(ng-repeat="form in forms",ng-class="{highlight:button.perform===form._id}")
            a(ng-click="setForm(form._id)") {{form.title}}